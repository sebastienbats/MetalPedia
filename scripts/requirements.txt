# ═══════════════════════════════════════════════════════════
# DÉPENDANCES PYTHON METALPEDIA - VERSIONS ÉPINGLÉES
# ═══════════════════════════════════════════════════════════
# Ces versions ont été testées ensemble pour éviter :
# - Les conflits de résolution pip
# - Le bug "unexpected keyword argument 'proxy'" de gotrue 2.9+
# ═══════════════════════════════════════════════════════════

requests==2.31.0
python-dotenv==1.0.0
tqdm==4.66.1

# Client Supabase et SES SOUS-DÉPENDANCES ÉPINGLÉES
# (gotrue DOIT rester en 2.4.3 max pour éviter le bug proxy)
supabase==2.3.4
postgrest==0.15.1
gotrue==2.4.3
storage3==0.7.4
httpx==0.25.2
